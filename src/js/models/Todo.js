export default class Todo {
    constructor({todoName, todo, todoColor,date}) {
        this.todoName = todoName;
        this.todo = todo;
        this.todoColor = todoColor;
        this.date = date;
    }
}